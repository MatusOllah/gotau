package gotau

/*
func isFreqTableFile(_path string) bool {
	switch strings.ToLower(path.Ext(_path)) {
	case ".frq", ".dio", ".llsm", ".pmk": // the common ones
		return true
	case ".goofy": // GOOFER
		if strings.HasSuffix(_path, "_features.goofy") {
			return true
		}
	}
	return false
}
*/
